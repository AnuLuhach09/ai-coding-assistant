# Base URL of the backend API, including the /api prefix.
# Example for Railway: https://my-backend.up.railway.app/api
# Leave unset only if the frontend is served from the same origin as the
# backend (relative /api requests).
VITE_API_URL="http://localhost:5000/api"
